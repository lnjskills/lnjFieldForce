# Example environment variables for this project
# Copy this file to .env and fill values. Never commit real secrets to the repository.

# Dev server (optional override)
VITE_PORT=8082

# API / backend
VITE_API_URL=http://localhost:3000/api

# Authentication (OIDC / Auth provider)
VITE_AUTH_CLIENT_ID=your-auth-client-id
VITE_AUTH_DOMAIN=your-auth-domain
VITE_AUTH_AUDIENCE=your-api-audience

# Feature flags
VITE_ENABLE_SENTRY=false

# Monitoring / Sentry (optional)
VITE_SENTRY_DSN=

# Third-party keys (only put publishable keys here; keep secret keys out of repo)
VITE_GOOGLE_MAPS_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=

# Misc
REACT_APP_NODE_ENV=development

# Notes:
# - Copy this file to .env (gitignored) and populate real values.
# - For CI / hosting (Vercel/Netlify/GitHub Actions) set these env vars in the host/dashboard.
# - Do not store private keys or SSH keys in the repo. Rotate any keys that were previously exposed.